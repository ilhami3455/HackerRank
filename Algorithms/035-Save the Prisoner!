def saveThePrisoner(n, m, s):
    a = (m + s - 1) % n
    if a == 0:
        return n
    else:
        return a
t = int(input())
for t_itr in range(t):
    nms = input().split()
    n = int(nms[0])
    m = int(nms[1])
    s = int(nms[2])
    result = saveThePrisoner(n, m, s)
    print(result)
